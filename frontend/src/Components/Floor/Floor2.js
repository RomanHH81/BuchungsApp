import React from 'react'
import { connect } from 'react-redux'

export const Floor2 = (props) => {
  return (
    <div>Floor2</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Floor2)